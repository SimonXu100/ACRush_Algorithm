//按照行数扫描,确定是哪一行的
class Solution {
    public String convert(String s, int numRows) {
       int n = s.length();
       char[] nums = s.toCharArray();
       char[] ans = new char[n];
       int gap = 2*numRows-2;
       
    }
}
